export function setSpeakers(state, speakers) {
  state.speakers = speakers;
}

export function setEvents(state, events) {
  state.events = events;
}

export function setSpeaker(state, speaker) {
  state.speaker = speaker;
}
